name: Build worlo Teams installers

on:
  workflow_dispatch:
  push:
    branches: [main, master]
    paths:
      - 'worlo-teams-desktop/**'
      - 'teams-app/**'
      - 'worlo-tines.css'
      - 'worlo-landing.css'
      - 'worlo-config.js'

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Build portable Windows zip
        run: powershell -ExecutionPolicy Bypass -File scripts/build-teams-portable.ps1
      - uses: actions/upload-artifact@v4
        with:
          name: worlo-teams-windows
          path: |
            downloads/worlo-teams-win.zip
            downloads/worlo-teams-mac.zip

  build-mac:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Build portable Mac zip
        run: |
          npm ci --prefix worlo-teams-desktop
          pwsh -File scripts/build-teams-portable.ps1 || bash -c 'echo use build script on mac'
      - uses: actions/upload-artifact@v4
        with:
          name: worlo-teams-mac
          path: downloads/worlo-teams-mac.zip
